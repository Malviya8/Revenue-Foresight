# Demo-only alias. Same pins as requirements.txt (includes Streamlit + Plotly).
# pip install -r requirements-demo.txt
# Scoring CLI: pip install -r requirements.txt still works; extra UI packages are unused.

-r requirements.txt
